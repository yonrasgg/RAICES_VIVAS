---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: weekly
title: "Semana 11 — 2026"
sprint: Sprint-02
week_start: 2026-03-09
week_end: 2026-03-15
tags:
  - weekly
  - reporte
---

# 📅 Semana 11 — 2026

| Campo          | Valor           |
| -------------- | --------------- |
| **Sprint**     | Sprint-02       |
| **Inicio**     | 2026-03-09      |
| **Fin**        | 2026-03-15      |

---

## 🎯 Objetivo de la Semana

- Formalizar la decisión de stack tecnológico (ADR-008) y la gobernanza cultural (ADR-009)
- Avanzar los modelos ER de los tres módulos (EDU, SAB, SAL)
- Investigar conectividad de territorios indígenas para dimensionar la estrategia offline-first

---

## ✅ Completadas esta Semana

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤", completed AS "📅 Completada"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'done' AND completed >= @week_start AND completed <= @week_end AND path LIKE '05-Sprints%'
ORDER BY completed ASC
```

---

## 🔄 En Progreso

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤", due AS "📅 Límite"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND (status = 'in-progress' OR status = 'review') AND path LIKE '05-Sprints%'
ORDER BY due ASC
```

---

## 📋 Pendientes para Esta Semana

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤", priority AS "Prioridad", due AS "📅 Límite"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'todo' AND due >= @week_start AND due <= @week_end AND path LIKE '05-Sprints%'
ORDER BY due ASC
```

---

## 🚧 Bloqueos

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'blocked' AND path LIKE '05-Sprints%'
ORDER BY name ASC
```

- RSK-002 mencionado: dependencia de conectividad en territorio Cabécar podría afectar validación de campo.

---

## 📊 Métricas de la Semana

```sqlseal
SELECT
  SUM(CASE WHEN (type='task' OR type='subtask') AND path LIKE '05-Sprints%' AND status='done' AND completed >= @week_start AND completed <= @week_end THEN 1 ELSE 0 END) as "✅ Semana",
  SUM(CASE WHEN (type='task' OR type='subtask') AND path LIKE '05-Sprints%' AND (status='in-progress' OR status='review') THEN 1 ELSE 0 END) as "🔄 En curso",
  SUM(CASE WHEN (type='task' OR type='subtask') AND path LIKE '05-Sprints%' AND status='blocked' THEN 1 ELSE 0 END) as "🚫 Bloqueadas",
  SUM(CASE WHEN type='adr' AND path LIKE '01-Proyecto/Decisiones%' AND "date" >= @week_start AND "date" <= @week_end THEN 1 ELSE 0 END) as "🏗️ ADR",
  SUM(CASE WHEN type='meeting' AND path LIKE '07-Reuniones%' AND "date" >= @week_start AND "date" <= @week_end THEN 1 ELSE 0 END) as "📝 Reuniones",
  SUM(CASE WHEN (type='task' OR type='subtask') AND path LIKE '05-Sprints%' AND status='done' THEN 1 ELSE 0 END) || '/' || SUM(CASE WHEN (type='task' OR type='subtask') AND path LIKE '05-Sprints%' THEN 1 ELSE 0 END) || ' (' || ROUND(100.0 * SUM(CASE WHEN (type='task' OR type='subtask') AND path LIKE '05-Sprints%' AND status='done' THEN 1 ELSE 0 END) / MAX(1, SUM(CASE WHEN (type='task' OR type='subtask') AND path LIKE '05-Sprints%' THEN 1 ELSE 0 END))) || '%)' as "📈 Progreso"
FROM files
```

---

## ⚠️ Riesgos Activos (snapshot)

```sqlseal
SELECT name AS "ID", title AS "Riesgo", severity AS "Severidad", status AS "Estado"
FROM files
WHERE type = 'risk' AND status = 'open' AND path LIKE '01-Proyecto/Riesgos%'
ORDER BY severity DESC
```

---

## 📝 Notas y Decisiones

- **ADR-008 aprobado (Stack Tecnológico):** Se formalizó la decisión de PWA con React + PouchDB + CouchDB + i18next. El análisis FODA ponderado mostró ventaja sobre Flutter y Vue para el contexto offline-first. Debate sobre si React Native sería mejor se descartó por mayor mantenimiento.
- **ADR-009 aprobado (Gobernanza Cultural):** Elkin presentó los principios CARE (Collective benefit, Authority to control, Responsibility, Ethics) aplicados al manejo de saberes ancestrales. Se definieron 4 niveles de acceso: público, comunidad, restringido-cultural, sagrado. Se requiere consentimiento comunitario para nivel restringido y sagrado.
- **Modelos ER:** Se avanzó en la definición de entidades para los módulos:
  - EDU: 6 entidades principales (Recurso, Lengua, CategoríaEducativa, Actividad, ProgresoEstudiante, MediaContent)
  - SAB: 7 entidades con niveles CARE integrados (SaberAncestral, Comunidad, Territorio, NivelAcceso, Custodio, Validación, MediaSaber)
  - SAL: 6 entidades (BrigadaSalud, CentroSalud, IndicadorComunitario, AlertaSanitaria, PersonalSalud, RegistroVisita)
- **Investigación de conectividad:** Geovanny compiló datos de SUTEL y FONATEL sobre cobertura 3G/4G en territorios indígenas. Resultado: 17 de 24 territorios tienen cobertura parcial, 7 sin cobertura estable → confirma necesidad de arquitectura offline-first con sync oportunista.
- Santiago comenzó a delinear los flujos de las pantallas SAL para preparar wireframes
- 📋 **Reunión formal:** [[07-Reuniones/MIN-004|MIN-004 — Aprobación de ADRs y Avance de Modelos ER]] (2026-03-14)

---

## 📋 Plan para la Próxima Semana

- Iniciar diseño de wireframes por módulo (EDU en Excalidraw primero)
- Preparar instrumentos de validación (cuestionario SUS + checklist accesibilidad)
- Definir la épica transversal EPIC-TRANS para funcionalidades cross-cutting
- Avanzar en la compilación del Avance-2

---

## 🔗 Enlaces

- [[00-Dashboard/Home|Dashboard]]
- [[00-Dashboard/Métricas|Métricas]]
- [[05-Sprints/Backlog|Backlog]]
