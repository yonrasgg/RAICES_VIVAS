---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: daily-note
date: 2026-03-10
author: Geovanny
tags:
  - daily
  - reporte
---

# 📅 2026-03-10 — Daily Note

---

## 🔄 Tareas en Progreso

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤", status AS "Estado"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND (status = 'in-progress' OR status = 'review') AND path LIKE '05-Sprints%'
ORDER BY id ASC
```

---

## 📋 Tareas Pendientes para Hoy

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤", priority AS "Prioridad"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'todo' AND due <= @date AND path LIKE '05-Sprints%'
ORDER BY priority DESC
```

---

## 🚧 Bloqueos

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'blocked' AND path LIKE '05-Sprints%'
ORDER BY id ASC
```

- Ningún bloqueo activo.

---

## 📊 Métricas del Día

```sqlseal
SELECT
  SUM(CASE WHEN status = 'done' AND completed = @date THEN 1 ELSE 0 END) as "✅ Hoy",
  SUM(CASE WHEN status IN ('in-progress', 'review') THEN 1 ELSE 0 END) as "🔄 En curso",
  SUM(CASE WHEN status = 'blocked' THEN 1 ELSE 0 END) as "🚫 Bloqueadas",
  SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) || '/' || COUNT(*) || ' (' || ROUND(100.0 * SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) / MAX(1, COUNT(*))) || '%)' as "📈 Progreso"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND path LIKE '05-Sprints%'
```

---

## ⚠️ Riesgos Activos (snapshot)

```sqlseal
SELECT id AS "ID", title AS "Riesgo", severity AS "Severidad", status AS "Estado"
FROM files
WHERE type = 'risk' AND status = 'open' AND path LIKE '01-Proyecto/Riesgos%'
ORDER BY severity DESC
```

---

## 📝 Notas e Ideas

### ADR-008 — Stack Tecnológico aprobado

Hoy se formalizó la decisión de stack como ADR-008 tras la evaluación de la semana pasada. Resultado final:

- **Frontend:** React (PWA con Workbox para service workers)
- **Almacenamiento local:** PouchDB (offline-first, sync nativo con CouchDB)
- **Sincronización:** CouchDB (replicación bidireccional cuando hay conexión)
- **Internacionalización:** i18next (soporte multi-idioma con carga dinámica)
- **Backend API:** Node.js + Express (endpoints REST para admin y sync)

El análisis FODA ponderado dio puntuaciones:
- React + PouchDB: **4.45 / 5.00**
- Flutter + Hive: 3.60 / 5.00
- Vue + SQLite: 3.85 / 5.00

Factores decisivos: ecosistema maduro de PWA con React, sincronización out-of-the-box con PouchDB↔CouchDB, y soporte nativo de i18next para lenguas indígenas.

### Modelos ER — Inicio

Se distribuyó el trabajo de modelos ER:
- **Elkin** arranca ER del módulo EDU (T-022)
- **Santiago** arranca ER del módulo SAB (T-023)
- **Geovanny** arranca ER del módulo SAL (T-024)

Se acordó que las entidades deben seguir la nomenclatura definida en los RF y que las relaciones reflejen los flujos del usuario.

> 📋 La aprobación formal de ADR-008 y el estado de los modelos ER se registran en [[07-Reuniones/MIN-004|MIN-004]] (sesión del 14 de marzo).
