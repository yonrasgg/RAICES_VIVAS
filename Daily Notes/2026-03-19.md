---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: daily-note
date: 2026-03-19
author: Geovanny
tags:
  - daily
  - reporte
---

# 📅 2026-03-19 — Daily Note

---

## 🔄 Tareas en Progreso

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Tarea",
  assignee AS "👤",
  status AS "Estado"
FROM "05-Sprints"
WHERE (type = "task" OR type = "subtask")
  AND (status = "in-progress" OR status = "review")
SORT id ASC
```

---

## 📋 Tareas Pendientes para Hoy

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Tarea",
  assignee AS "👤",
  priority AS "Prioridad"
FROM "05-Sprints"
WHERE (type = "task" OR type = "subtask")
  AND status = "todo"
  AND due <= date("2026-03-19")
SORT priority DESC
```

---

## 🚧 Bloqueos

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Tarea",
  assignee AS "👤"
FROM "05-Sprints"
WHERE (type = "task" OR type = "subtask") AND status = "blocked"
SORT id ASC
```

- Las entrevistas de validación (T-030) se posponen a Sprint-03: la coordinación con las comunidades requiere más tiempo logístico del esperado.

---

## 📊 Métricas del Día

```dataviewjs
const today = dv.current().date;
const tasks = dv.pages('"05-Sprints"').where(t => t.type === "task" || t.type === "subtask");

const doneToday = tasks.where(t =>
  t.status === "done" && t.completed &&
  dv.date(t.completed).toString() === dv.date(today).toString()
).length;

const inProgress = tasks.where(t => t.status === "in-progress" || t.status === "review").length;
const blocked = tasks.where(t => t.status === "blocked").length;

const parseH = (v) => { if (!v) return 0; if (typeof v === "number") return v; const m = String(v).match(/\d+/); return m ? parseInt(m[0]) : 0; };
let hoursToday = 0;
for (const t of tasks.where(t => t.status === "done" && t.completed && dv.date(t.completed).toString() === dv.date(today).toString())) {
  hoursToday += t.effort_actual ? parseH(t.effort_actual) : parseH(t.effort);
}

const totalTasks = tasks.length;
const totalDone = tasks.where(t => t.status === "done").length;
const pct = totalTasks > 0 ? Math.round((totalDone / totalTasks) * 100) : 0;

dv.table(
  ["📊 Métrica", "Valor"],
  [
    ["✅ Completadas hoy", doneToday],
    ["⏱️ Horas ejecutadas", `${hoursToday}h`],
    ["🔄 En progreso", inProgress],
    ["🚫 Bloqueadas", blocked],
    ["📈 Progreso global", `${pct}% (${totalDone}/${totalTasks})`]
  ]
);
```

---

## ⚠️ Riesgos Activos (snapshot)

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Riesgo",
  severity AS "Severidad",
  status AS "Estado"
FROM "01-Proyecto/Riesgos"
WHERE type = "risk" AND status = "open"
SORT severity DESC
```

---

## 📝 Notas e Ideas

### Wireframes — Planificación y primeros bocetos

Sesión de trabajo sobre diseño de pantallas:

**EDU — Wireframes (T-026, Geovanny):**
- 5 pantallas definidas: Dashboard educativo, Detalle de recurso, Progreso del estudiante, Búsqueda/filtros, Perfil/configuración
- Navegación decidida: **bottom-tab** con 4 secciones (Inicio, Explorar, Progreso, Perfil)
- Se priorizó legibilidad en pantallas pequeñas (smartphones Android gama baja)

**SAB — Wireframes (T-027, Elkin):**
- 5 pantallas: Catálogo de saberes, Ficha de saber ancestral, Mapa de comunidades, Solicitud de acceso, Panel de custodio
- La ficha de saber incluye indicador visual del nivel CARE (color-coded)

**SAL — Wireframes (T-028, Santiago):**
- 5 pantallas: Dashboard comunitario, Registro de brigada, Alertas sanitarias, Indicadores de salud, Perfil centro de salud
- Se discutió la integración del mapa offline para ubicar brigadas

### Instrumentos de validación (T-029, Santiago)

Santiago presentó la estructura de los instrumentos:
1. **Guión semi-estructurado** para entrevistas con docentes, custodios y auxiliares de salud
2. **Cuestionario SUS adaptado** (System Usability Scale) — 10 preguntas con escala Likert 1-5
3. **Checklist de accesibilidad** WCAG 2.1 nivel AA — 15 criterios aplicables a PWA móvil

Se acordó que los instrumentos se aplicarán sobre mockups de baja fidelidad en Sprint-03.

> 📋 El estado de wireframes e instrumentos se revisará en la reunión del sábado → [[07-Reuniones/MIN-003|MIN-003]].
