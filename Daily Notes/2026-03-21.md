---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: daily-note
date: 2026-03-21
author: Geovanny
tags:
  - daily
  - reporte
---

# 📅 2026-03-21 — Daily Note

---

## 🔄 Tareas en Progreso

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤", status AS "Estado"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND (status = 'in-progress' OR status = 'review') AND path LIKE '05-Sprints%'
ORDER BY id ASC
```

---

## 📋 Tareas Pendientes para Hoy

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤", priority AS "Prioridad"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'todo' AND due <= @date AND path LIKE '05-Sprints%'
ORDER BY priority DESC
```

---

## 🚧 Bloqueos

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'blocked' AND path LIKE '05-Sprints%'
ORDER BY id ASC
```

- Ningún bloqueo activo.

---

## 📊 Métricas del Día

```sqlseal
TEMPLATE
| 📊 Métrica | Valor |
|---|---|
{{#each data}}
| ✅ Completadas hoy | {{this.done_today}} |
| 🔄 En progreso | {{this.in_progress}} |
| 🚫 Bloqueadas | {{this.blocked}} |
| 📈 Progreso global | {{this.pct}}% ({{this.total_done}}/{{this.total_tasks}}) |
{{/each}}

SELECT
  SUM(CASE WHEN status = 'done' AND completed = @date THEN 1 ELSE 0 END) as done_today,
  SUM(CASE WHEN status IN ('in-progress', 'review') THEN 1 ELSE 0 END) as in_progress,
  SUM(CASE WHEN status = 'blocked' THEN 1 ELSE 0 END) as blocked,
  COUNT(*) as total_tasks,
  SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) as total_done,
  ROUND(100.0 * SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) / MAX(1, COUNT(*))) as pct
FROM files
WHERE (type = 'task' OR type = 'subtask') AND path LIKE '05-Sprints%'
```

---

## ⚠️ Riesgos Activos (snapshot)

```sqlseal
SELECT id AS "ID", title AS "Riesgo", severity AS "Severidad", status AS "Estado"
FROM files
WHERE type = 'risk' AND status = 'open' AND path LIKE '01-Proyecto/Riesgos%'
ORDER BY severity DESC
```

---

## 📝 Notas e Ideas

### Compilación del Avance-2

Se inició la revisión y compilación del documento de Avance-2:

**Secciones completadas:**
1. Resumen ejecutivo — actualizado con logros del Sprint-02
2. Marco teórico — se expandió con la sección de saberes ancestrales y el contexto digital en territorios
3. Arquitectura — referencia a ADR-008 (stack), ADR-009 (gobernanza), modelos ER por módulo
4. Stack tecnológico — incluye hardware recomendado, software, y matriz de conectividad
5. Roadmap — actualizado con 5 sprints planificados y distribución de stories

**Secciones pendientes:**
- Wireframes finales (dependen de ejecución visual — quedan como especificaciones)
- Resultados de validación (se posponen a Sprint-03 por logística territorial)
- Presupuesto actualizado con horas reales del Sprint-02

### Sprint-02 Review preparado

Se redactó el Sprint-02 Review con:
- Tabla de lo completado vs lo pendiente
- Evidencias y artefactos producidos
- Feedback del equipo
- Lecciones aprendidas (mini-retro)

**Conclusión del equipo:** El sprint tuvo foco fuerte en planificación y decisiones de diseño (ADR-008, ADR-009, ER models, stack), pero la ejecución de diagramas visuales y wireframes quedó como especificación, no como artefacto final. Se propone que Sprint-03 arranque con ejecución directa de lo planificado.

### Sincronización Jira

Se verificó que todos los issues están sincronizados entre el vault y Jira:
- 48 items en vault con Jira keys asignados
- 3 sprints futuros creados (Sprint-03 ID:67, Sprint-04 ID:68, Sprint-05 ID:69)
- 7 stories nuevas asignadas a sus sprints correspondientes
- EPIC-TRANS (RV-47) con prioridad Highest

> Todo alineado para la entrega del 1 de abril.

> 📋 Revisión formal del sprint y preparación de Avance-2 documentada en [[07-Reuniones/MIN-003|MIN-003]].
