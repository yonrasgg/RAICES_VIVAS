---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: daily-note
date: 2026-03-17
author: Geovanny
tags:
  - daily
  - reporte
---

# 📅 2026-03-17 — Daily Note

---

## 🔄 Tareas en Progreso

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤", status AS "Estado"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND (status = 'in-progress' OR status = 'review') AND path LIKE '05-Sprints%'
ORDER BY name ASC
```

---

## 📋 Tareas Pendientes para Hoy

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤", priority AS "Prioridad"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'todo' AND due <= @date AND path LIKE '05-Sprints%'
ORDER BY priority DESC
```

---

## 🚧 Bloqueos

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'blocked' AND path LIKE '05-Sprints%'
ORDER BY name ASC
```

- Ningún bloqueo activo.

---

## 📊 Métricas del Día

```sqlseal
SELECT
  SUM(CASE WHEN status = 'done' AND completed = @date THEN 1 ELSE 0 END) as "✅ Hoy",
  SUM(CASE WHEN status IN ('in-progress', 'review') THEN 1 ELSE 0 END) as "🔄 En curso",
  SUM(CASE WHEN status = 'blocked' THEN 1 ELSE 0 END) as "🚫 Bloqueadas",
  SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) || '/' || COUNT(*) || ' (' || ROUND(100.0 * SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) / MAX(1, COUNT(*))) || '%)' as "📈 Progreso"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND path LIKE '05-Sprints%'
```

---

## ⚠️ Riesgos Activos (snapshot)

```sqlseal
SELECT name AS "ID", title AS "Riesgo", severity AS "Severidad", status AS "Estado"
FROM files
WHERE type = 'risk' AND status = 'open' AND path LIKE '01-Proyecto/Riesgos%'
ORDER BY severity DESC
```

---

## 📝 Notas e Ideas

### Creación de EPIC-TRANS y User Stories

Sesión de planificación para definir la épica transversal y las historias de usuario para Sprint 03-05.

**EPIC-TRANS — Funcionalidades Transversales:**
- Motor de localización (i18next con soporte para bribri, cabécar, ngäbere, español)
- Sync Engine (PouchDB ↔ CouchDB con replicación oportunista)
- Capa de gobernanza cultural (4 niveles CARE con verificación de rol)
- Vinculado a 3 RF transversales (RF-TRANS-01, RF-TRANS-02, RF-TRANS-03) y 4 RNF (RNF-01, RNF-02, RNF-03, RNF-04)

**User Stories creadas:**
| ID | Título | SP | Sprint | Asignado |
|----|--------|---:|--------|----------|
| US-TRANS-01 | Motor de localización | 8 | Sprint-03 | Geovanny |
| US-TRANS-02 | Sync offline-first | 5 | Sprint-03 | Elkin |
| US-EDU-04 | Sistema de progreso | 5 | Sprint-04 | Geovanny |
| US-EDU-05 | Conversión offline contenido | 3 | Sprint-04 | Santiago |
| US-SAB-03 | Niveles CARE en UI | 3 | Sprint-04 | Elkin |
| US-SAL-03 | Brigadas de salud móvil | 5 | Sprint-04 | Santiago |
| US-SAL-05 | Dashboard comunitario | 3 | Sprint-05 | Geovanny |

Total: **32 SP** distribuidos en 3 sprints.

### Sprint Planning futuro

Se redactaron los documentos de planning para Sprint-03, Sprint-04 y Sprint-05:
- **Sprint-03 (Abr):** Implementación piloto EDU + funcionalidades transversales — 16 SP
- **Sprint-04 (May):** Módulos SAB + SAL + motor educativo — 16 SP
- **Sprint-05 (Jun):** Integración, testing E2E y despliegue piloto — 3 SP + tareas de QA

> 📋 Sesión de planificación documentada en [[07-Reuniones/MIN-005|MIN-005]].
