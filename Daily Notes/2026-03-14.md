---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: daily-note
date: 2026-03-14
author: Geovanny
tags:
  - daily
  - reporte
---

# 📅 2026-03-14 — Daily Note

---

## 🔄 Tareas en Progreso

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤", status AS "Estado"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND (status = 'in-progress' OR status = 'review') AND path LIKE '05-Sprints%'
ORDER BY name ASC
```

---

## 📋 Tareas Pendientes para Hoy

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤", priority AS "Prioridad"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'todo' AND due <= @date AND path LIKE '05-Sprints%'
ORDER BY priority DESC
```

---

## 🚧 Bloqueos

```sqlseal
SELECT name AS "ID", title AS "Tarea", assignee AS "👤"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'blocked' AND path LIKE '05-Sprints%'
ORDER BY name ASC
```

- Ningún bloqueo activo.

---

## 📊 Métricas del Día

```sqlseal
SELECT
  SUM(CASE WHEN status = 'done' AND completed = @date THEN 1 ELSE 0 END) as "✅ Hoy",
  SUM(CASE WHEN status IN ('in-progress', 'review') THEN 1 ELSE 0 END) as "🔄 En curso",
  SUM(CASE WHEN status = 'blocked' THEN 1 ELSE 0 END) as "🚫 Bloqueadas",
  SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) || '/' || COUNT(*) || ' (' || ROUND(100.0 * SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) / MAX(1, COUNT(*))) || '%)' as "📈 Progreso"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND path LIKE '05-Sprints%'
```

---

## ⚠️ Riesgos Activos (snapshot)

```sqlseal
SELECT name AS "ID", title AS "Riesgo", severity AS "Severidad", status AS "Estado"
FROM files
WHERE type = 'risk' AND status = 'open' AND name LIKE 'RSK%'
ORDER BY severity DESC
```

---

## 📝 Notas e Ideas

### Investigación de conectividad territorial

Geovanny presentó al equipo los hallazgos de la investigación de conectividad en territorios indígenas costarricenses:

**Fuentes consultadas:**
- SUTEL — Mapas de cobertura móvil 2024
- FONATEL — Informes de avance CPC 2023-2024
- MEP — Diagnóstico de centros educativos en territorios

**Hallazgos principales:**
- De los 24 territorios indígenas de Costa Rica, **17 tienen cobertura 3G/4G parcial** (señal intermitente, depende de ubicación)
- **7 territorios tienen conectividad nula o casi nula** (Chirripó, Alto Chirripó, Tayní, Telire, China Kichá zona alta, Salitre zona alta, Cabagra zona alta)
- La velocidad promedio donde hay 3G es de **0.5-2 Mbps** — suficiente para sync de JSON pero no para streaming de video
- Dispositivos más comunes: smartphones Android gama baja (1-2 GB RAM), algunos tablets del MEP

**Implicaciones arquitectónicas:**
- Confirma la necesidad de **offline-first** como requisito no negociable (RNF-01)
- El sync engine debe ser **oportunista**: sincronizar cuando detecte conexión, sin depender de ella
- Los contenidos multimedia deben ser **pre-descargados y comprimidos** (imágenes < 200KB, audio < 1MB)
- Se recomienda **CouchDB con replicación incremental** para minimizar tráfico

### ADR-009 formalizado

Se completó el texto del ADR-009 (Gobernanza Cultural) con los 4 niveles CARE y el protocolo de consentimiento comunitario. Aprobado por el equipo.

> 📋 Los hallazgos de conectividad y la aprobación de ambos ADR se registraron formalmente en [[07-Reuniones/MIN-004|MIN-004]].
