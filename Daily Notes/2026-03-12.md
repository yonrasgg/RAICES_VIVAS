---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: daily-note
date: 2026-03-12
author: Geovanny
tags:
  - daily
  - reporte
---

# 📅 2026-03-12 — Daily Note

---

## 🔄 Tareas en Progreso

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤", status AS "Estado"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND (status = 'in-progress' OR status = 'review') AND path LIKE '05-Sprints%'
ORDER BY id ASC
```

---

## 📋 Tareas Pendientes para Hoy

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤", priority AS "Prioridad"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'todo' AND due <= @date AND path LIKE '05-Sprints%'
ORDER BY priority DESC
```

---

## 🚧 Bloqueos

```sqlseal
SELECT id AS "ID", title AS "Tarea", assignee AS "👤"
FROM files
WHERE (type = 'task' OR type = 'subtask') AND status = 'blocked' AND path LIKE '05-Sprints%'
ORDER BY id ASC
```

- Ningún bloqueo activo.

---

## 📊 Métricas del Día

```sqlseal
TEMPLATE
| 📊 Métrica | Valor |
|---|---|
{{#each data}}
| ✅ Completadas hoy | {{this.done_today}} |
| 🔄 En progreso | {{this.in_progress}} |
| 🚫 Bloqueadas | {{this.blocked}} |
| 📈 Progreso global | {{this.pct}}% ({{this.total_done}}/{{this.total_tasks}}) |
{{/each}}

SELECT
  SUM(CASE WHEN status = 'done' AND completed = @date THEN 1 ELSE 0 END) as done_today,
  SUM(CASE WHEN status IN ('in-progress', 'review') THEN 1 ELSE 0 END) as in_progress,
  SUM(CASE WHEN status = 'blocked' THEN 1 ELSE 0 END) as blocked,
  COUNT(*) as total_tasks,
  SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) as total_done,
  ROUND(100.0 * SUM(CASE WHEN status = 'done' THEN 1 ELSE 0 END) / MAX(1, COUNT(*))) as pct
FROM files
WHERE (type = 'task' OR type = 'subtask') AND path LIKE '05-Sprints%'
```

---

## ⚠️ Riesgos Activos (snapshot)

```sqlseal
SELECT id AS "ID", title AS "Riesgo", severity AS "Severidad", status AS "Estado"
FROM files
WHERE type = 'risk' AND status = 'open' AND path LIKE '01-Proyecto/Riesgos%'
ORDER BY severity DESC
```

---

## 📝 Notas e Ideas

### ADR-009 — Gobernanza Cultural — discusión

Sesión de trabajo con Elkin sobre el marco de gobernanza para datos culturales sensibles. Puntos clave:

- **Principios CARE** adoptados como base: Collective benefit, Authority to control, Responsibility, Ethics
- Se definieron **4 niveles de acceso** al conocimiento:
  1. **Público:** Información general sobre programas educativos, salud, eventos
  2. **Comunidad:** Datos accesibles solo para miembros verificados de la comunidad
  3. **Restringido-Cultural:** Saberes que requieren aprobación del custodio cultural
  4. **Sagrado:** Información que NUNCA se digitaliza — solo referencia a que existe

- Elkin enfatizó que la digitalización de saberes ancestrales debe respetar la oralidad como medio legítimo, no solo como fuente de datos.

### Modelos ER — Avance

- Elkin presentó borrador de ER para EDU: 6 entidades (Recurso, Lengua, CategoríaEducativa, Actividad, ProgresoEstudiante, MediaContent) con relaciones N:M entre Recurso y Lengua
- Santiago revisó las entidades de SAB: 7 entidades con la integración del campo `nivel_acceso` en SaberAncestral, vinculado a NivelAcceso
- Geovanny avanzó SAL: 6 entidades con enlace a EBAIS (CentroSalud) como entidad externa referenciada

### Pendiente

- Formalizar ADR-009 con el texto completo esta semana
- Completar diagramas Mermaid de los 3 ER antes del viernes

> 📋 Estos avances se presentarán en la sesión de aprobación del sábado → [[07-Reuniones/MIN-004|MIN-004]]
