---
banner_src: "08-Recursos/Imágenes/cover-proyecto.png"
banner_src_x: 0.47714
banner_src_y: 0.42
type: weekly
title: "Semana 10 — 2026"
sprint: Sprint-02
week_start: 2026-03-02
week_end: 2026-03-08
tags:
  - weekly
  - reporte
---

# 📅 Semana 10 — 2026

| Campo          | Valor           |
| -------------- | --------------- |
| **Sprint**     | Sprint-02       |
| **Inicio**     | 2026-03-02      |
| **Fin**        | 2026-03-08      |

---

## 🎯 Objetivo de la Semana

- Arrancar formalmente el Sprint-02 con todas las tareas asignadas en Jira
- Iniciar el diagrama de contexto C4 (T-021) y la evaluación de stack tecnológico (T-025)
- Establecer cadencia de reuniones semanales y dailies

---

## ✅ Completadas esta Semana

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Tarea",
  assignee AS "👤",
  completed AS "📅 Completada"
FROM "05-Sprints"
WHERE (type = "task" OR type = "subtask")
  AND status = "done"
  AND completed >= date(this.week_start)
  AND completed <= date(this.week_end)
SORT completed ASC
```

---

## 🔄 En Progreso

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Tarea",
  assignee AS "👤",
  due AS "📅 Límite"
FROM "05-Sprints"
WHERE (type = "task" OR type = "subtask")
  AND (status = "in-progress" OR status = "review")
SORT due ASC
```

---

## 📋 Pendientes para Esta Semana

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Tarea",
  assignee AS "👤",
  priority AS "Prioridad",
  due AS "📅 Límite"
FROM "05-Sprints"
WHERE (type = "task" OR type = "subtask")
  AND status = "todo"
  AND due >= date(this.week_start)
  AND due <= date(this.week_end)
SORT due ASC
```

---

## 🚧 Bloqueos

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Tarea",
  assignee AS "👤"
FROM "05-Sprints"
WHERE (type = "task" OR type = "subtask") AND status = "blocked"
SORT id ASC
```

- Sin bloqueos reportados esta semana.

---

## 📊 Métricas de la Semana

```dataviewjs
const ws = dv.current().week_start;
const we = dv.current().week_end;
const tasks = dv.pages('"05-Sprints"').where(t => t.type === "task" || t.type === "subtask");

const doneThisWeek = tasks.where(t =>
  t.status === "done" && t.completed &&
  dv.date(t.completed) >= dv.date(ws) &&
  dv.date(t.completed) <= dv.date(we)
).length;

const inProgress = tasks.where(t => t.status === "in-progress" || t.status === "review").length;
const blocked = tasks.where(t => t.status === "blocked").length;

const parseH = (v) => { if (!v) return 0; if (typeof v === "number") return v; const m = String(v).match(/\d+/); return m ? parseInt(m[0]) : 0; };
let hoursWeek = 0;
for (const t of tasks.where(t => t.status === "done" && t.completed && dv.date(t.completed) >= dv.date(ws) && dv.date(t.completed) <= dv.date(we))) {
  hoursWeek += t.effort_actual ? parseH(t.effort_actual) : parseH(t.effort);
}

const adrs = dv.pages('"01-Proyecto/Decisiones"').where(d => d.type === "adr" && d.date && dv.date(d.date) >= dv.date(ws) && dv.date(d.date) <= dv.date(we)).length;
const meetings = dv.pages('"07-Reuniones"').where(m => m.type === "meeting" && m.date && dv.date(m.date) >= dv.date(ws) && dv.date(m.date) <= dv.date(we)).length;

const totalTasks = tasks.length;
const totalDone = tasks.where(t => t.status === "done").length;
const pct = totalTasks > 0 ? Math.round((totalDone / totalTasks) * 100) : 0;

dv.table(
  ["📊 Métrica", "Valor"],
  [
    ["✅ Completadas esta semana", doneThisWeek],
    ["⏱️ Horas ejecutadas", `${hoursWeek}h`],
    ["🔄 En progreso", inProgress],
    ["🚫 Bloqueadas", blocked],
    ["🏗️ ADR esta semana", adrs],
    ["📝 Reuniones", meetings],
    ["📈 Progreso global", `${pct}% (${totalDone}/${totalTasks})`]
  ]
);
```

---

## ⚠️ Riesgos Activos (snapshot)

```dataview
TABLE WITHOUT ID
  id AS "ID",
  title AS "Riesgo",
  severity AS "Severidad",
  status AS "Estado"
FROM "01-Proyecto/Riesgos"
WHERE type = "risk" AND status = "open"
SORT severity DESC
```

---

## 📝 Notas y Decisiones

- Reunión de arranque del Sprint-02 el lunes 2 de marzo: se revisó el backlog completo y se asignaron las primeras tareas
- Geovanny inició el levantamiento de actores y sistemas adyacentes para el diagrama C4 (T-021)
- El equipo discutió las opciones de stack tecnológico: se acordó evaluar React vs Vue vs Flutter para el frontend, y PouchDB vs SQLite para offline-first
- Elkin revisó la documentación del módulo SAB para preparar su modelo ER (T-023)
- Santiago identificó los flujos principales del módulo SAL para mapear entidades (T-024 asignado a Geovanny, pero SAL es su dominio de conocimiento)
- Se definió que el stack se formalizará como ADR-008 la próxima semana

---

## 📋 Plan para la Próxima Semana

- Completar evaluación FODA del stack tecnológico y formalizar ADR-008
- Avanzar modelos ER por módulo (EDU, SAB, SAL)
- Discutir principios de gobernanza cultural con Elkin (preparación ADR-009)
- Continuar diagrama C4 con los sistemas adyacentes definidos

---

## 🔗 Enlaces

- [[00-Dashboard/Home|Dashboard]]
- [[00-Dashboard/Métricas|Métricas]]
- [[05-Sprints/Backlog|Backlog]]
